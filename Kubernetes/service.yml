apiVersion: v1
kind: Service
metadata:
  name: netflix-app-vr-svc
  namespace: vr-1-ns
  labels:
    app: netflix-app-vr-svc
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
  selector:
    app: netflix-app-vr
